#include <stdio.h>

int main() {
  // Task: scan a character and print its ASCII code
  /* 
    EXAMPLE:
    Input: A
    Output: A -> 65
  */
  
  scanf("%c", &symbol);
  printf("%c -> %d", symbol, symbol);
  return 0;
}
